//Comentário única linha
/* Comentário linhas infinitas até o fechamento do comentário,
 usa-se muito dentro do código css, js*/
 // <!-- <meta name="viewport" content="width=device-width, initial-scale=1.0"> --> comentário para linguagem de marcação html 

//  var init = express();

/*
bx + c = 0

f(500+1) = bx. + c */


var preco = 2;
var desconto = 0.2;
preco -= desconto // or preco = preco - desconto

console.log(preco)

