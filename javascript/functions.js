function execute(a, b) {

return a + b

}

const recipe =  execute(500, 20)

// console.log('My Gold: ' + recipe)

// FN + Loops

// Criando entrada para arrays e filtrando apenas números pares.

function activeValue(arr) {

    let evenNums = []

for(let i = 0; i < arr.length; i++) {

    if(arr[i] % 2 == 0) {

        evenNums.push(arr[i])

    }

}
console.log(evenNums)

}



const put = 500 + 500
const socket = [50, 23, 25, 26, 41, 60,]
socket.push(put)

activeValue(socket)